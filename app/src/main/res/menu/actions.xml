<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
  <item
        android:id="@+id/menu_create_income"
        android:title="create income"
        app:showAsAction="never"
    />

  <item
      android:id="@+id/menu_add_income"
      android:title="add income"
      app:showAsAction="never"
      />

  <item
      android:id="@+id/menu_transfer"
      android:title="transfer funds"
      app:showAsAction="never"
      />

    <item
        android:id="@+id/menu_create_transaction"
        android:title="make transaction"
        app:showAsAction="never"
        />

  <item
      android:id="@+id/menu_delete_db"
      android:title="clear DB"
      app:showAsAction="never"
      />

    <item
        android:id="@+id/menu_view_transactions"
        android:title="View Transactions "
        app:showAsAction="ifRoom"
        />

      <item
          android:id="@+id/menu_set_value"
          android:title="Set model value"
          app:showAsAction="ifRoom"
      />

</menu>
